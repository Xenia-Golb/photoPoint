import { HashRouter } from "react-router-dom";
import { Layout } from "./shared/ui/Layout";

function App() {
  return (
    <HashRouter>
      <Layout></Layout>
    </HashRouter>
  );
}

export default App;
